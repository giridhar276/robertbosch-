# Database Project: SQLite Inventory Management

A menu-driven Python project using the built-in `sqlite3` module.

## Features
- Creates the database and table automatically
- Inserts sample products
- Adds, lists, searches, updates and deletes products
- Shows inventory summary

## Run
```bash
python main.py
```

The database is created at `data/inventory.db`.
